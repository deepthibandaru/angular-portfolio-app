[build]
  command = "npm run build"
  publish = "dist/angularexampleapp/browser"

[context.production.environment]
  NODE_VERSION = "22"

[build.environment]
  # Add any build-time environment variables here if needed
  # EXAMPLE_VAR = "value"